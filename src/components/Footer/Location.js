import React from "react";

const Location = (props) => {
  return (
    <div>
      <div className="footer-top">{props.text}</div>
      <div className="footer-divider"></div>
    </div>
  );
};

export default Location;
